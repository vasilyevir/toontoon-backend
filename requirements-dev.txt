# Зависимости замера, не прода.
#
# Всё отсюда нужно только скриптам в `scripts/` — тем, что снимают кадры и
# считают по ним числа. В образ приложения это не попадает: веса моделей
# распознавания весят сотни мегабайт, а на боевом сервере они не нужны ни разу.
#
#     uv pip install -r requirements-dev.txt
#
# Сходство лица числом, а не мнением судьи (`scripts/eval_frames.py`).
#
# Судья по сходству двоичен: он отвечает либо 10, либо 0, промежуточных
# значений почти нет — это подтвердилось на трёх независимых замерах. Как
# тревога «человека потеряли» он годится, как оценка — нет, и медиана из трёх
# вызовов этого не лечит. ArcFace даёт косинус между лицами: число, которое не
# упирается в потолок и не зависит от настроения модели.
insightface==0.7.3
onnxruntime==1.20.1
