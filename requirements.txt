fastapi==0.141.1
uvicorn[standard]==0.34.0
redis==5.2.1
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
httpx==0.28.1
python-multipart==0.0.31
argon2-cffi==25.1.0
# Web Push (VAPID) — app/services/push_service.py
pywebpush==2.3.0
py-vapid==1.9.4
# Sign in with Apple — app/services/apple_oauth.py (JWKS signature verification)
PyJWT[crypto]==2.13.0
# PostgreSQL — долговременное хранилище (app/db). Redis остаётся для сессий,
# кэша, rate-limit и локов; всё, что нельзя потерять, живёт здесь.
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
# Объектное хранилище (app/storage): MinIO локально и в кластере, тот же код
# работает с любым S3-совместимым провайдером.
aioboto3==13.2.0
# Миниатюры и срезание EXIF при загрузке (в фото с телефона лежат координаты).
Pillow==12.3.0
markdown==3.7
